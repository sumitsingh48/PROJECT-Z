const express = require('express');
const router = express.Router();

let course = [];
let id = 1;


router.get('/',(req,res)=>{
    res.render('course',{
        title: 'course'
    });
});

//get all courses

router.get('/api',( req,res )=>{
    res.json(course);
});
